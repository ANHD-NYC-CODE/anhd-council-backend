version: '2'
services:
  app:
    restart: always
    build: .
    expose:
      - "8000"
    container_name: "app"
    image: debian/latest
    links:
      - postgres
      - redis
    depends_on:
      - postgres
      - redis
    ports:
      - '8000:8000'
    networks:
      - network1
      - nginx_network
    volumes:
      - ./:/app
      - ./data:/app/data
      - static_volume:/app/static
      - ./logs:/app/logs
    entrypoint: ["sh", "/app/docker-entrypoint.sh"]
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=app.settings.production
  celery_default:
    restart: always
    build: .
    container_name: "celery_default"
    networks:
      - network1
    links:
      - redis
      - postgres
    depends_on:
      - postgres
      - redis
      - celerybeat
    volumes:
      - ./:/app
      - ./data:/app/data
      - ./logs:/app/logs
      - ./celery:/app/celery
    env_file:
      - .env
    entrypoint: "celery -A app worker -Q celery -l debug -n celery_worker --concurrency=2 --logfile=./celery/logs/default.log"
  celery_update:
    restart: always
    build: .
    container_name: "celery_update"
    networks:
      - network1
    depends_on:
      - postgres
      - redis
      - celerybeat
    volumes:
      - ./:/app
      - ./data:/app/data
      - ./logs:/app/logs
      - ./celery/logs:/app/celery/logs
    links:
      - postgres
      - redis
    env_file:
      - .env
    entrypoint: "celery -A app worker -Q update -l debug -n update_worker --concurrency=2 --logfile=./celery/logs/update.log"
  celerybeat:
    restart: always
    build: .
    container_name: "celerybeat"
    entrypoint: "celery -A app beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler --logfile=./celery/logs/beat.log"
    networks:
      - network1
    depends_on:
      - postgres
      - redis
    links:
      - postgres
      - redis
    volumes:
      - ./:/app
      - ./celery:/app/celery
    env_file:
      - .env
  # https://www.distributedpython.com/2018/10/13/flower-docker/
  flower:
    image: mher/flower
    container_name: "flower"
    environment:
      - CELERY_BROKER_URL=redis://redis
      - FLOWER_PORT=8888
    ports:
      - '8888':'8888'
    networks:
      - network1
      - nginx_network
    depends_on:
      - redis
  redis:
    hostname: redis
    container_name: "redis"
    image: redis:alpine
    volumes:
      - redis_data:/data
    networks:
      - network1
    env_file:
      - .env
  postgres:
    image: postgres:11
    container_name: "postgres"
    # https://blog.2ndquadrant.com/basics-of-tuning-checkpoints/
    command: ["-c","huge_pages=try", "-c","effective_io_concurrency=1", "-c", "max_stack_depth=6MB", "-c", "maintenance_work_mem=512MB", "-c", "temp_buffers=128MB", "-c", "shared_buffers=1024MB", "-c", "checkpoint_timeout=30min", "-c", "checkpoint_completion_target=0.93", "-c", "max_wal_size=2048MB", "-c", "effective_cache_size=1024MB", "-c", "work_mem=512MB", "-c", "max_connections=200", "-c", "random_page_cost=1"]
    shm_size: '2GB'
    environment:
      - POSTGRES_DB=anhd
      - POSTGRES_USER=anhd
    networks:
      - network1
    volumes:
      - pg_vol1:/var/lib/postgresql/data
    env_file:
      - .env
  nginx:
    build: ./nginx
    container_name: "nginx_server"
volumes:
  pg_vol1:
  redis_data:
  static_volume:
networks:
  network1:
  nginx_network:
    driver: bridge
